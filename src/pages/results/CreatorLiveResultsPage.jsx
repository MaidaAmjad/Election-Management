import { ROUTES } from '../../utils/constants';
import LiveResultsPage from './LiveResultsPage';

export default function CreatorLiveResultsPage() {
  return <LiveResultsPage listPath={ROUTES.CREATOR_RESULTS} />;
}
