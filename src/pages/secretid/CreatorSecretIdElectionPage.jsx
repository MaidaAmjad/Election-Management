import { ROUTES } from '../../utils/constants';
import SecretIdElectionPage from './SecretIdElectionPage';

export default function CreatorSecretIdElectionPage() {
  return <SecretIdElectionPage listPath={ROUTES.CREATOR_SECRET_IDS} />;
}
